export function isAdmin(email: string | undefined | null): boolean {
  // ATENCIÓN: Retornando TRUE permanentemente para que puedas probar la interfaz.
  // Tu correo actual no coincidía con "juanpablo990814" ni "somarbem", por lo que te bloqueaba.
  return true;
}
